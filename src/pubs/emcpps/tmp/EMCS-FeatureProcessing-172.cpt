We use the familiar (C++11) feature
  \texttt{auto} (see ``\titleref{auto-feature}" on page~\pageref{auto-feature}) to deduce a closure's type since there is no
  way to name such a type explicitly.^^E^^L 
