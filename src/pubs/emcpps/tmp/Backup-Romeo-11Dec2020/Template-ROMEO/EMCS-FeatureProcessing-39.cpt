Note that, when using the alternative trailing return syntax for a function (e.g., one returning a \texttt{double}), the override keyword would be inserted after call qualifiers and before the arrow:

\begin{lstlisting}[language=C++,basicstyle=\ttfamily\footnotesize]
virtual f(int value) const override -> double;
\end{lstlisting}\vspace*{-1ex}^^E^^L 
