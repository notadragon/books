Note that the default underlying type
of an \texttt{enum}~\texttt{class} is ubiquitously \texttt{int}, and
it is not implementation defined; see ``\titleref{enumclass}" on page~\pageref{enumclass}.^^E^^L 
